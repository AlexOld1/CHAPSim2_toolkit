name: chapsim2-toolkit
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - numpy>=1.20.0
  - matplotlib>=3.3.0
  - pandas>=1.2.0
  - tqdm>=4.60.0
  - pip
  - pip:
    - -e .
# Optional: uncomment for 3D visualization (visualise_3d.py)
#  - pyvista>=0.32.0
